// Values to be assigned via Update State action on Apply Search click

// Key: <searchParams> (the name of the current screen's searchParams
// state variable: see note in parse_search_params.js)

{"searchColumn": "{{ [Search Form].Fields.search_column }}", "searchString": "{{ [Search Form].Fields.search_string }}"}

// Key: searchFormValues (parsed to set the default values of the two
// search form fields on each open)

{"search_column": "{{ [Search Form].Fields.search_column }}", "search_string": "{{ [Search Form].Fields.search_string }}"}
